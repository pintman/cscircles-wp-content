#!/bin/bash

# init defaults
#cat /wordpress.sql | mysql -h mysql --defaults-extra-file=/etc/mysql/debian.cnf

#service apache2 start
#echo "using DB PW $MYSQL_ROOT_PASSWORD"

while true; do
    sleep 99;
done
